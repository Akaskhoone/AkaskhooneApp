import variable from 'src/theme/variables/commonColor';

export default (variables = variable) => {
  const ImageFieldTheme = {
    overflow: 'hidden',
    borderWidth: 0,
    alignSelf: 'center',
    margin: 20,
    height: null,
    width: null
  };
  return ImageFieldTheme;
};
